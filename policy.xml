<policies>
    <inbound>
        <base />
        <!-- No authentication needed here - backend handles Azure OpenAI auth -->
        <!-- APIM only validates subscription key from frontend -->
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>